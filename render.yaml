services:
  - type: web
    name: omnissiah-engine-v3
    env: node
    plan: free
    buildCommand: bash render-build.sh
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
